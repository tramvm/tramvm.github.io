name:
  workspace.tramvm.com
on:
  push:
    branches:
      - master
jobs:
  build_deploy:
    name: Workspace Website
    runs-on: self-hosted
    steps:
      - name: echo path
        run: "echo $PWD"
      - name: Call script
        env:
          name: prod
        run: "docker-compose up -d --build"
